"""LogPPT (Le & Zhang 2023) — local fine-tuned RoBERTa-base for log-template extraction.

Runs through `transformers` against weights downloaded to the local
HuggingFace cache. Stage D of plan 001's local-only neural track.
"""
